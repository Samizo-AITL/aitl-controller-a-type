title: AITL Controller A-Type
description: Three-layer control architecture (PID × FSM × LLM) educational controller
lang: en

url: "https://samizo-aitl.github.io"
baseurl: "/aitl-controller-a-type"

# IMPORTANT → Disable Minima theme because SCSS imports handle it
# theme: minima

markdown: kramdown
kramdown:
  input: GFM
  math_engine: mathjax
  syntax_highlighter: rouge
  hard_wrap: false
  auto_ids: true

highlighter: rouge

plugins:
  - jekyll-feed
  - jekyll-seo-tag

permalink: pretty

exclude:
  - Gemfile
  - Gemfile.lock
