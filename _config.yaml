title: AITL Controller A-Type
description: Three-layer control architecture (PID × FSM × LLM) educational controller
lang: en

# GitHub Pages URL settings
url: "https://samizo-aitl.github.io"
baseurl: "/aitl-controller-a-type"

# Theme (Minima disabled so custom layout works)
# theme: minima

# Markdown / MathJax settings
markdown: kramdown
kramdown:
  input: GFM
  math_engine: mathjax
  syntax_highlighter: rouge
  hard_wrap: false
  auto_ids: true

highlighter: rouge

plugins:
  - jekyll-feed
  - jekyll-seo-tag

permalink: pretty

exclude:
  - Gemfile
  - Gemfile.lock
